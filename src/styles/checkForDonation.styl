$errorRed = #ab0000;
$checkingBackgroundColor = #291a05;
$notFoundBackgroundColor = $errorRed;

.check-for-donation {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.check-for-donation__card {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 1em 2em;
  border-radius: 10px;
  background-color: $checkingBackgroundColor;

  span {
    margin-top: 1em;
    text-align: center;
  }

  .loader {
    margin: 20px;
    width: 50px;
    height: 50px;
  }
}

.check-for-donation__card--verified {
  background-color: $checkingBackgroundColor;
}

.check-for-donation__card--not-found {
  .check-for-donation__icon {
    border-radius: 100%;
    background-color: $errorRed;
    color: white;
    line-height: 55px;
  }
}

.check-for-donation__icon {
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 20px;
  width: 40px;
  height: 40px;
  font-size: 60px;
}