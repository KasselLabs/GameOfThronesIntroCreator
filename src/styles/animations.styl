$TIME_FACTOR = 1;
$start_at = 0 * $TIME_FACTOR;

@keyframes animationNormalFade {
  0% { opacity: 0; }
  12% { opacity: 1; }
  90% { opacity: 1; }
  100% { opacity: 0; }
}

@keyframes logoAnimation {
  0% {
    opacity: 1;
  }
  100% {
    opacity: 1;
    transform: translateY(-.5em) scaleX(0.95) scaleY(0.95);
  }
}

$strokeSize= 2px;
$strokeBlur= 2px;
$blackAlpha= #00000040;
$colorAlpha= #bf833940;
$whiteAlpha= #ffffff80;
$animationTextColor= #dcbc94;

.animate {
  text-shadow:
    -1px -1px $strokeBlur $blackAlpha,
    -1px 0 $strokeBlur $blackAlpha,
    -1px 2px $strokeBlur $blackAlpha,
    0 2px $strokeBlur black,
    2px 2px $strokeBlur black,
    2px 0 $strokeBlur black,
    1px -1px $strokeBlur $blackAlpha,
    0 -1px $strokeBlur $blackAlpha,
    -1px -1px 2px $colorAlpha,
    -1px 0 2px $colorAlpha,
    0 -1px 2px $colorAlpha,
    -1px -1px 1px $whiteAlpha;
  color: $animationTextColor;
  white-space: pre-line;
  letter-spacing: .4em;
  line-height: 1.5;

  .video-overlay__content {
    display: flex;
    justify-content: center;
    align-items: center;

    > div {
      display: flex;
      align-items: center;
      justify-content: center;
      opacity: 0;
      animation-fill-mode: forwards;
      // background-color: rgba(255,255,255,0.2); // uncomment to show text box for development
    }
  }

  #text0  { animation: animationNormalFade $TIME_FACTOR * 2s linear $TIME_FACTOR * 4.4s - $start_at; }
  #text1  { animation: animationNormalFade $TIME_FACTOR * 2s linear $TIME_FACTOR * 6.7s - $start_at; }
  #text2  { animation: animationNormalFade $TIME_FACTOR * 2s linear $TIME_FACTOR * 9s - $start_at; }
  #text3  { animation: animationNormalFade $TIME_FACTOR * 2s linear $TIME_FACTOR * 14.5s - $start_at; }
  #text4  { animation: animationNormalFade $TIME_FACTOR * 2s linear $TIME_FACTOR * 16.8s - $start_at; }
  #text5  { animation: animationNormalFade $TIME_FACTOR * 2s linear $TIME_FACTOR * 19s - $start_at; }
  #text6  { animation: animationNormalFade $TIME_FACTOR * 2s linear $TIME_FACTOR * 21.5s - $start_at; }
  #text7  { animation: animationNormalFade $TIME_FACTOR * 2s linear $TIME_FACTOR * 24.9s - $start_at; }
  #text8  { animation: animationNormalFade $TIME_FACTOR * 2s linear $TIME_FACTOR * 27.2s - $start_at; }
  #text9  { animation: animationNormalFade $TIME_FACTOR * 2s linear $TIME_FACTOR * 29.8s - $start_at; }
  #text10 { animation: animationNormalFade $TIME_FACTOR * 2s linear $TIME_FACTOR * 32.2s - $start_at; }
  #text11 { animation: animationNormalFade $TIME_FACTOR * 2s linear $TIME_FACTOR * 35.2s - $start_at; }
  #text12 { animation: animationNormalFade $TIME_FACTOR * 2s linear $TIME_FACTOR * 38.3s - $start_at; }
  #text13 { animation: animationNormalFade $TIME_FACTOR * 2s linear $TIME_FACTOR * 40.6s - $start_at; }
  #text14 { animation: animationNormalFade $TIME_FACTOR * 2s linear $TIME_FACTOR * 42.7s - $start_at; }
  #text15 { animation: animationNormalFade $TIME_FACTOR * 2s linear $TIME_FACTOR * 45s - $start_at; }
  #text16 { animation: animationNormalFade $TIME_FACTOR * 2s linear $TIME_FACTOR * 48.2s - $start_at; }
  #text17 { animation: animationNormalFade $TIME_FACTOR * 2s linear $TIME_FACTOR * 50.6s - $start_at; }
  #text18 { animation: animationNormalFade $TIME_FACTOR * 2s linear $TIME_FACTOR * 56s - $start_at; }
  #text19 { animation: animationNormalFade $TIME_FACTOR * 2s linear $TIME_FACTOR * 59.2s - $start_at; }
  #text20 { animation: animationNormalFade $TIME_FACTOR * 2s linear $TIME_FACTOR * 61.1s - $start_at; }
  #text21 { animation: animationNormalFade $TIME_FACTOR * 2s linear $TIME_FACTOR * 65.4s - $start_at; }
  #text22 { animation: animationNormalFade $TIME_FACTOR * 2s linear $TIME_FACTOR * 67.5s - $start_at; }
  #text23 { animation: animationNormalFade $TIME_FACTOR * 2s linear $TIME_FACTOR * 69.5s - $start_at; }
  #text24 { animation: animationNormalFade $TIME_FACTOR * 2s linear $TIME_FACTOR * 72.5s - $start_at; }
  #text25 { animation: animationNormalFade $TIME_FACTOR * 2s linear $TIME_FACTOR * 79.7s - $start_at; }

  #text26 {
    color: rgb(255, 243, 180);
    text-shadow: rgba(255, 255, 255, 0.43) 0px 0px 30px, rgb(196, 158, 58) 2px 2px 6px, rgb(196, 158, 58) 2px -2px 6px, rgb(196, 158, 58) -2px 2px 6px, rgb(196, 158, 58) -2px -2px 6px, rgb(160, 94, 29) 0px 0px 20px, rgb(230, 190, 53) -3px 0px 2px, rgb(230, 190, 53) 3px 0px 2px;
    letter-spacing: 0;
    font-size: 2.5em;
    font-family: GameOfThrones;
    animation: logoAnimation $TIME_FACTOR * 2.500s linear $TIME_FACTOR * 88.6s - $start_at;
    // opacity: 1;
  }

  #text27 { animation: animationNormalFade $TIME_FACTOR * 2.056s linear $TIME_FACTOR * 94s - $start_at; }
  #text28 { animation: animationNormalFade $TIME_FACTOR * 2.056s linear $TIME_FACTOR * 96.2s - $start_at; }

}

.pause-animation {
  #text0 { animation-play-state: paused }
  #text1 { animation-play-state: paused }
  #text2 { animation-play-state: paused }
  #text3 { animation-play-state: paused }
  #text4 { animation-play-state: paused }
  #text5 { animation-play-state: paused }
  #text6 { animation-play-state: paused }
  #text7 { animation-play-state: paused }
  #text8 { animation-play-state: paused }
  #text9 { animation-play-state: paused }
  #text10 { animation-play-state: paused }
  #text11 { animation-play-state: paused }
  #text12 { animation-play-state: paused }
  #text13 { animation-play-state: paused }
  #text14 { animation-play-state: paused }
  #text15 { animation-play-state: paused }
  #text16 { animation-play-state: paused }
  #text17 { animation-play-state: paused }
  #text18 { animation-play-state: paused }
  #text19 { animation-play-state: paused }
  #text20 { animation-play-state: paused }
  #text21 { animation-play-state: paused }
  #text22 { animation-play-state: paused }
  #text23 { animation-play-state: paused }
  #text24 { animation-play-state: paused }
  #text25 { animation-play-state: paused }
  #text26 { animation-play-state: paused }
  #text27 { animation-play-state: paused }
  #text28 { animation-play-state: paused }
}
