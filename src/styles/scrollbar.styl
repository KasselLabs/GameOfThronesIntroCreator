::-webkit-scrollbar {
    width: 10px;
}

::-webkit-scrollbar-track {
    background-color: darken($themeDarkColor, 30%);
    border-radius: 10px;
}

::-webkit-scrollbar-thumb {
    border-radius: 10px;
    background-color: lighten($themeDarkColor, 10%);
    &:hover {
        background-color: lighten($themeDarkColor, 20%);
    }
}